import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    levelCard: {
        width: '100%',
        backgroundColor: '#282828',
        marginBottom: 15,
        padding: 15,
        borderRadius: 10,
    },
    levelCardInfo: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#fff'
    }
});
