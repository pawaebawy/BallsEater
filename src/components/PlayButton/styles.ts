import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    playButton: {
        fontFamily: 'Montserrat-Bold',
        fontSize: 42,
        fontWeight: 'bold',
        color: '#fff'
    }
});
